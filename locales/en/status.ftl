status-current-model = Current model: { $model }
status-token-info =
    { $model } API key:

    { $token }
status-token-not-set =
    { $model } API key is not configured.
    Configure it: /setup
status-prompt-info =
    { $model } system prompt:

    { $prompt }
status-prompt-not-set =
    { $model } system prompt is not configured.
    Configure it: /setup
status-decrypt-error =
    Error decrypting the token for { $model }.
    Reconfigure it: /setup
